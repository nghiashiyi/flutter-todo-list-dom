name: flutter_todo_list_dom
description: A new Flutter project.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: '>=2.19.2 <3.0.0'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  riverpod: ^2.2.0
  flutter_riverpod: ^2.2.0
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  freezed: ^2.3.2
  equatable: ^2.0.5
  dartz: ^0.10.1
  get_it: ^7.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
  build_runner: ^2.3.3
  hive_generator: ^2.0.0

flutter:
  uses-material-design: true